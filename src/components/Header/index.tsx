import React from "react";
import "./style.scss";

const HeaderContainer = () => {
  return (
    <div className="header-container">
      <div className="title">ChatFlow</div>
      <button className="save-btn">Save Changes</button>
    </div>
  );
};

export default HeaderContainer;
